{
 "id": "S2d8-20-strictjson-400",
 "purpose": "Gate-authorized golden (recorded fresh): a non-JSON /v1/messages body is rejected client-side with the strict-JSON 400 shape before any routing; zero upstream hits.",
 "anchor": "CLIProxyAPI v7.3.4, commit 8335eac731946bd4eff18f500653f93736df53d6 (docker eceasy/cli-proxy-api:v7.3.4, digest sha256:97825da3009f98acf78b5c172fde650a5fbe7a690950a69ce6d7b535d77d4266)",
 "recorded_at": "2026-09-16T04:08:31",
 "record": "downstream status+headers+body; upstream.jsonl (must be EMPTY)",
 "config": "run/config.s2d8.yaml (gemini-api-key mock-gem-key -> host.docker.internal:19001, models gemini-mock-model/gm)",
 "http_status": "400",
 "upstream_hits": 0,
 "dynamic_fields": [
  "Date, X-Cpa-Trace-Id, Content-Length headers",
  "reference/mock port numbers in upstream.jsonl Host fields",
  "content_block.id digits in stream tool_use events (process counter; orchestrator ruling S2d8-2 accepts masking)",
  "terminal transport-error text if it differs from 'unexpected EOF' (record what the reference emits)",
  "ports (worker-local): reference 18317, mock 19001"
 ],
 "byte_exact_fields": [
  "message_start.usage.input_tokens (o200k_base estimate): NOT masked; asserted byte-exactly per orchestrator ruling S2d8-1 \u2014 record the actual value in the fixture; do NOT list it as a masked dynamic field"
 ],
 "malformed_request_body": true,
 "malformed_body_probe": "this is not json (genuinely non-JSON, not truncated JSON \u2014 truncated JSON is parsed leniently per S2d2 round-1 evidence)",
 "expected_vs_recorded_deviation": "REALITY vs admitted spec \u00a72.1: the reference did NOT emit the \u00a72.1 OpenAI-shaped envelope {'error':{'message':'Invalid request: <err>','type':'invalid_request_error'}}. It emitted the Claude envelope {'type':'error','error':{'type':'invalid_request_error','message':'unknown provider for model'}} with NO trailing space and NO model name (hex-verified). Status 400 + Content-Type application/json match. Recorded bytes are authoritative evidence for the spec side to reconcile \u00a72.1/NE-LENIENT."
}