{
 "id": "S1-26",
 "purpose": "Cross-check golden (cla2gem impl-review N6, routed via S1): pin that POST /v1/messages accepts ALL FIVE client credential transports (query ?key=, query ?auth_token=, X-Goog-Api-Key, non-Bearer verbatim Authorization), plus the non-Bearer-scheme rejection. S1-07 already pinned X-Api-Key on this route; this closes the remaining transports on the Claude surface.",
 "anchor": "CLIProxyAPI v7.3.4, commit 8335eac731946bd4eff18f500653f93736df53d6, image digest sha256:97825da3009f98acf78b5c172fde650a5fbe7a690950a69ce6d7b535d77d4266",
 "recorded_at": "2026-09-16T04:46:46",
 "record": "response (per request: status + headers + exact body)",
 "config": "S1 baseline (BOOTSTRAP.md section 3, no providers)",
 "dynamic_fields": [
  "Date",
  "X-Cpa-Trace-Id"
 ],
 "requests": [
  {
   "name": "qkey-messages",
   "method": "POST",
   "path": "/v1/messages?key=oracle-local-key-1",
   "http_status": "400"
  },
  {
   "name": "qauthtoken-messages",
   "method": "POST",
   "path": "/v1/messages?auth_token=oracle-local-key-1",
   "http_status": "400"
  },
  {
   "name": "xgoog-messages",
   "method": "POST",
   "path": "/v1/messages",
   "http_status": "400"
  },
  {
   "name": "verbatim-auth-messages",
   "method": "POST",
   "path": "/v1/messages",
   "http_status": "400"
  },
  {
   "name": "badscheme-messages",
   "method": "POST",
   "path": "/v1/messages",
   "http_status": "401"
  }
 ],
 "notes": [
  "Plain baseline config (BOOTSTRAP.md section 3); NO provider attachment needed - every accepted-auth probe terminates at routing with 400 model_not_found (claude envelope), which is itself the proof that the auth gate passed.",
  "Same request body for all probes so the only variable is the credential transport.",
  "Source basis being pinned: internal/api/server_routes.go attaches ONE AuthMiddleware(accessManager) to the /v1 group (and /v1beta); internal/access/config_access/provider.go reads the same five transports for every request through that middleware, and extractBearerToken compares a non-Bearer Authorization header value verbatim."
 ]
}