# Adversary review — S2d4 (cla2oai), gate round 1
Reviewer: adv-spec-4. VERDICT: FAIL — 1 BLOCKING (two-stage thinking table false — same class as S2d2 B3, template fix exists); all 5 token counts independently reproduced; 22/22 fixtures verified.

BLOCKING (rulings embedded):
B1. §3.2 thinking table = translator-stage mapping presented as final. Effective final values (validate.go + provider/openai + mapConfiguredHighIntent): disabled->low (not none); budget 0->low; budget -1->medium; enabled-no-budget->medium; budget 1..512->low; >=24577->high; budget<-1 -> 400 'budget N cannot be converted to a valid level'; adaptive-with-effort: none->low/auto->medium/minimal->low/xhigh->high/max->high/unknown->400; adaptive-without-effort -> xhigh (passes through). Cross-evidence: S2d2-thinking-clamps/invalid goldens (same executor+provider shape). RULING: mirror S2d2 §3.3's two-stage EFFECTIVE-mapping table; record the discriminating goldens (disabled, budget 0/300/30000, budget -5 400, adaptive-with-effort, countTokens-with-bad-budget 400) — oracle-runner-4, one pass.
NON-BLOCKING: N1 DD-model rewrite one-liner (+optional golden); N2 estimate collector completeness (extra part types counted-not-translated — declare claude_input_tokens.go normative); N3 stream tool-use precision (sanitized ids; empty-args no-delta; deterministic ascending-index finalize — NO volatility entry needed); N4 §3.2 edges (empty-join->Raw JSON content; nothing-survives->no message; reminder flush order); N5 content-type table (middleware charset vs WriteErrorResponse bare); N6 next-batch recordables (stream truthiness, top_p-when-temperature-absent, B1 discriminating rows, auth-transport cross-ref to S2d2's golden instead of recording); N7 summarizer mechanism sentence inverted (compact form when cut does NOT fire; verbatim when cut leaves invalid JSON); N8 citation nits (sjson marshaling; redacted Authorization note).
