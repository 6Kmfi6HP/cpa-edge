# Adversarial review — S2d7 (gem2cla), gate round 1
Reviewer: adv-spec-2. VERDICT: FAIL — 2 BLOCKING (small, bounded); otherwise exceptionally faithful: all 10 headline claims verified against source + fixtures; 22/25 fixtures read in detail.

BLOCKING (rulings embedded):
B1. Tokenizer ruling NOT incorporated: §2.5 final para + §7 item 6 still pose it as open. NOW REGISTERED AS R-TOK in SPEC.md §5. Fix: replace with the decided contract (js-tiktoken o200k_base; translated-Claude-body segments joined "\n": system, per-message role+text/tool fields, tools, tool_choice per helps/claude_input_tokens.go; totalTokens byte-exact =4 for S2d7-12; no masking).
B2. Recordable validator error paths lack exact byte specs + goldens. RULING: enumerate ALL exact strings (they are public-interface bytes): (a) countTokens validator 400s x7: "invalid Claude token count request JSON"; "...must be a JSON object"; "...messages must be a non-empty array"; "...messages must contain objects"; "...message role must be user or assistant"; "...message content must be a string or array"; "...content blocks must be typed objects" — record >=1 golden (e.g. role:system turn). (b) stream validator 502s x4: "claude executor: upstream returned empty stream response"; "...missing message_start"; "...ended before message completion"; "...malformed stream data" + fallback chain error.message -> error.type -> "unknown upstream error" + empty/"[DONE]" payloads SKIPPED by validator — record >=2 goldens (empty-stream + missing-message_delta). New recordings -> oracle-runner-3 (original recorder). Exemptions NOT granted: these are trivially scriptable.
NON-BLOCKING: N1 empty/unknown delta emits ONE empty-parts chunk (input_json_delta only returns nothing) — fix wording or add golden; N2 user_id precedence: prompt_cache_key > session_id/sessionId > conversation.id/conversation/conversation_id all ABOVE content: seed — complete list; N3 Retry-After:1 header claim missing (golden S2d7-19); N4 service_tier position in skeleton (after metadata, before top_p); N5 maxOutputTokens numeric strings parse ("500"->500; only non-numeric coerce 0); N6 tool-valid mock variant + S2d7-23/24 case-table rows; N7 usage-less message_delta emits STOP chunk w/ trafficType-only usageMetadata; N8 raw splice TRIMS whitespace; N9 merged signature-bearing thought parts: LAST signature wins.
