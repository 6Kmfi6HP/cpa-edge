# Adversary review — S2d3 (oai2cla), gate round 1
Reviewer: adv-spec-1. VERDICT: FAIL — 1 BLOCKING (assistant reasoning_content family omitted); section otherwise very strong: all 18 fixtures byte-verified, both recorded refinements confirmed, user_id hashes recomputed numerically.

BLOCKING (rulings embedded):
B1. §2.4 missing assistant reasoning_content handling: (a) DEFAULT: silently dropped — add to the per-message table or the Dropped-silently list (implementers need the rule); (b) models[].is-compat: true variant: executor prepends unsigned {"type":"thinking","thinking":<reasoning_content>,"signature":""} block + applies thinking-summary config (TranslateRequestWithAPIKeyModelCompatibility -> preserveEmptyThinkingBlocks + ApplySummaryConfigForModel). Config-gated + recordable. RULING: add both; record ONE is-compat golden.
FOLLOW-UP BATCH AUTHORIZED (-> oracle-runner-2, your original recorder): is-compat golden; thinking-config survival (models[].thinking block — config-only); fingerprint-profile claude-code-cli (maskable UUID); model-suffix probe ("cm(4096)"); stream-429 commit rule; empty-stream SSE headers + bare [DONE]; malformed-stream validation 5-message set; Retry-After forwarding.
NON-BLOCKING: N1 Connection mirrors client (close recorded, keep-alive default) — add clause. N2 = the authorized batch (record the two weak deferrals + suffix probe). N3 = the batch's four pinning gaps. N4 error-event fallback chain (error.message -> error.type -> "unknown upstream error") — add. N5 fix stale case-8 pre-recording text + meta purpose fields. N6 add R-SSE decoded-event cross-ref sentence. N7 citations ignored; stop:"" -> stop_sequences [""] edge.
OQ-3/4/5 RULING: keep MIRRORING the silent drops (n>1, legacy functions, unknown content parts) — documented recorded behavior; no SPEC §5 registration needed since CPA-Edge matches upstream.
