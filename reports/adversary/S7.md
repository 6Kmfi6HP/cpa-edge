# Adversary review — S7 (platform degradation), gate round 1
Reviewer: adv-spec-3. VERDICT: FAIL — 3 BLOCKING; content otherwise strong (all 4 recorded deviations verified; upstream evidence accurate; 15/15 goldens verified; R-S7-A/B/C coherent).

BLOCKING (with orchestrator rulings):
B1. TLS listener unclassified (config tls.enable/cert/key; server.go useTLS). RULING: add matrix row — node: EQUIVALENT (implementable) or declared-ABSENT; cloudflare/vercel: platform-terminated, config-accepted-no-op; + NE-S7-xx entry.
B2. CONTRADICTION: S7 "Redis RESP usage ABSENT on every runtime" vs S6 §4 byte-exact recorded contract + goldens S6-07/08/09 on disk. RULING: option (a) — node EQUIVALENT (raw-listener dispatch implementable in runtimes/node); cloudflare/vercel DEGRADED (no raw TCP listener; exact substitute behavior pinned); S6 §4 rescoped as node-runtime contract (S6 writer amends in its fix round; S6 gate checks consistency with this ruling).
B3. F5b/F5c device flows "EQUIVALENT every runtime" unfunded for serverless (poll goroutines + in-memory session registry die with invocation; multi-instance 404s). RULING: add substrate MUSTs — Store-backed OAuth session registry (S6 store-mapping row added), scheduled/queued poll execution (cloudflare: DO alarms; vercel: DEGRADED — pin exact observable semantics, e.g. auth-url 200 envelope as recorded, status stays wait until 30-min expiry, then unknown-or-expired), with S3/S6 cross-refs. Downgrade F5b/F5c rows on vercel accordingly.

NON-BLOCKING (rulings): N1 plugin quota route deferral line — add. N2 S5 §8.3 mask-secrets: RULING mirror upstream (values returned as stored); D1 deploy guide carries the security warning instead of a non-equivalence. N3 drop the trailing \n on 501 bodies (byte-consistency with gin family). N4 quote recorded DELETE-logs key order (message,removed,success). N5 reword F5b golden coverage (source-based). N6 add Home mode + control-panel adjacency lines. N7 local-client platform-applicability note (R-S7-A style). N8 F1 proxy-credentialed auto-refresh interaction one-liner (degraded per F1 rows). N9 meta.yaml dynamic_fields hygiene (future recordings).
