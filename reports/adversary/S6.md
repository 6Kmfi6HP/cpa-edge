# Adversary review — S6 (state & storage), gate round 1
Reviewer: adv-spec-6. VERDICT: FAIL — 6 BLOCKING (all text-level, no re-recording strictly required); 17/17 fixtures verified; Store mapping + Store abstraction compliance positive.

BLOCKING:
B1. §3.7 model catalog: only models.json documented; codex_client_models.json + devin_models.json updaters omitted (same 2-URL scheme, //go:embed fallback, startup+3h, 30s timeout, keep-old, revision counters). Fix §3.7.1 + §0 Store row (catalog:<provider> keys) + §3.7.2.
B2. §4 PING empty-payload frame is WRONG: recorded S6-08 conn1 = *2 $4 pong $-1 (nil bulk string), NOT $0. Fix to $-1.
B3. §3.1 config key tables miss disable-claude-cloak-mode (top-level bool, default false; pinned by S6-01 golden). Add it.
B4. §3.4 re-serialization over-claims single-line: metadata-map path + claude/codex/meta saves are single-line; xai/kimi/vertex refresh-path saves are INDENTED 2-space (SetIndent). Qualify by write-path.
B5. §3.4.2/§3.4.3 miss built-in vertex auth-file type: {service_account verbatim SA JSON, project_id, email, location?, type:"vertex", prefix?}, vertex-<sanitized-project>.json, written by POST /v0/management/vertex/import. Add to both + cross-ref S5's recorded fake-PEM golden (tests/fixtures/S5/S5-vertex-import/ STEP 9).
B6. GET /v0/management/logs?limit=abc -> 400 {"error":"invalid limit: must be a positive integer"} — in golden S6-13, absent from spec. Add to B7/§3.6.2/§5.
NON-BLOCKING: N1 effective-view omitempty semantics; N2 schema blocks mark omitempty fields; N3 xai device-flow structs checked-OK; N4 split 500 {"error":"handler unavailable"} vs 503 {"error":"core auth manager unavailable"}; N5 oauth-excluded-models removal + plugins.configs pruning; N6 name the config-writing endpoints + auth-state readers as S5-deferred (routing/strategy already in §3.1.3); N7 plugins.store-auth + plugins.auth-revision rows; N8 payload rule inner schema line; N9 load-failure: codex.live-media-relay.Validate + plugins-dir resolution aborts; LoadConfigOptional NormalizePluginsConfig; N10 ring capacity 1000 = CPA-Edge DESIGN choice (upstream forwarder queue 1024) — mark as ours; define LogRingEntry level closed set; N11 S6-10 meta "(verbatim upload)" label fix (oracle); N12 global alphabetical-map-marshal note for management JSON.
