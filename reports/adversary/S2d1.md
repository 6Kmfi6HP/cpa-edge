# Adversary review — S2d1 (oai2gem), gate round 1
Reviewer: adv-spec-6. VERDICT: FAIL — 1 BLOCKING (recordable behavior deferred without golden: multi-candidate streaming); otherwise unusually precise — omission audit found zero factual errors; 13 fixtures byte-verified.

BLOCKING (ruling embedded):
B1. Multi-candidate STREAMING (n>1 + stream:true) deferred with a convenience reason — violates R-FIXTURE (recordable => golden). RULING: RECORD IT — one C22 golden (stream, candidateCount=2, terminal chunk carrying usageMetadata + per-candidate finishReasons; optional variant where only candidate 1 has a finishReason) -> oracle-runner-2 (your recorder; mock fleet already supports per-model keyed stream events per your own required_extensions note). Exercises the high-risk unpinned quirks (usage filter keys on candidates.0.finishReason; per-candidate FunctionIndex/role state).
NON-BLOCKING (rulings): N1 reword the synthetic tool-response turn rule (parts per ASSISTANT tool_call id WITH a name; missing tool message still yields result:"{}"); N2 complete the request-fault classification (402 + model_not_found-code bodies also non-request-faults) or defer wholesale to S4 with a cross-ref; N3 one-line note: BuildErrorResponseBodyWithError's terminal-auth branch unreachable on this pipeline; N4 add the explicit R-SSE decoded-frame binding sentence; N5 name legacy functions/function_call in the dropped list; N6 mark the empty-stream edge OPTIONAL or fold into a future mission; N7 vertex bookkeeping confirmed (registry will track it as a pending mission); N8 meta hygiene good practice, no action.
