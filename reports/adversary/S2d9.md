# Adversary review — S2d9 (codex passthrough), gate round 1
Reviewer: adv-spec-5. VERDICT: FAIL — 2 BLOCKING (text-only; goldens already pin corrected behavior); otherwise exceptionally accurate; all 17 fixtures read.

BLOCKING (rulings embedded):
B1. Stream usage-detail injection MISSING + §7 "verbatim" claim FALSE: EVERY forwarded Responses stream chunk passes EnsureResponsesUsageDetails (helps.TranslateStreamWithClaudeInputTokens gates on FormatOpenAIResponse; skips response.compaction): terminal response.usage gains output_tokens_details.reasoning_tokens:0 + input_tokens_details.cached_tokens:0 — RECORDED in goldens 03 + 17. Fix §4.2's exception list + §7's claim; note per-frame scope + compaction skip + §4.7 same-mechanism-on-aggregate.
B2. §3.2 tools row INVERTS image-generation modes: injection happens ONLY in `off` (default); true/all strip + no injection; chat strips declared image tools on this route + no injection; passthrough preserves + no injection. One-line fix.
NON-BLOCKING (rulings): N1 remove "~1s default" — contradicts S2d5's recorded codex ~4s (S2D5-24); S4 owns the algorithm. N2 compact mutation list += derived prompt_cache_key + Session-Id + NormalizeCodexToolSchemas + normalizeCodexParallelToolCalls (golden 15 pins cache_key). N3 response.done renamed to response.completed before forwarding (never client-visible). N4 CRLF framing wording (\r\n\r\n preserved/extended). N5 framer repair = output-rebuild only (no id-hydration). N6 input-id prefix precision (non-empty only; re-prefix; collision hash suffix). N7 CONTENT-verbatim modulo sanitizers ([REDACTED] + 2048-rune truncation) — state caveat. N8 drop/reword the "unexpected EOF" attribution aside (it belongs to the gemini executor family). N9 add the cases-file-ports vs recorded-ports cross-pointer. N10 Lite flag accepts JSON string "true". N11 the 404->cooldown observation cites the wiped worker-1 run — flag as un-pinned wire-note input to S4 or record a pair. N12 error-body comparison: byte-exact IS feasible (deterministic re-marshal); upgrade the guidance.
