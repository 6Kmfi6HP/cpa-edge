# Adversary review — S2d8 (cla2gem), gate round 1
Reviewer: adv-spec-3. VERDICT: FAIL — 1 BLOCKING (thinking-config semantics contradict goldens 07/10); everything else verified as the tightest direction spec (all 16 rules, SSE machine, error semantics vs source + bytes).

BLOCKING (rulings embedded):
B1. §3.1/§1/§6 thinking-config vs recorded wire: goldens 07/10 carry request thinking {enabled,budget_tokens:1024} -> upstream "generationConfig":{} (thinkingConfig DELETED — executor capability pass: ResolvedModelInfo Thinking==nil -> StripThinkingConfig("gemini") leaves the EMPTY object). RULING: rewrite §3.1 as the two-stage rule (translator maps thinkingBudget -> generationConfig.thinkingConfig; THEN model-capability pass strips for capability-resolved models without thinking support — MUST, golden-pinned; user-defined/unresolved models keep it, cite apply.go IsUserDefinedModel); fix §1's "no-op" scope note; fix the §6 case-07/10 rows (they pin the STRIP). Also: oracle meta note for 07/10 (expected_upstream carried the pre-strip shape; recording pipeline missed the deviation) — oracle-owned files.
NON-BLOCKING: N1 M-extraction ladder: top-level message/type consulted ONLY when body error is NOT a JSON object; error-map-without-message/code -> raw body text (code_handlers.go). N2 §3.5.2 segment list non-exhaustive — declare claude_input_tokens.go the normative collector + list the extra segments (web_search/web_fetch/code_execution/server_tool_use/mcp_tool_use). N3 add the NE-LENIENT acknowledgment line (SPEC §5 applies to /v1/messages).
