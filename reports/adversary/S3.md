# Adversary review — S3 (auth flows), gate round 1
Reviewer: adv-spec-2. VERDICT: FAIL — 6 BLOCKING (all small/targeted), 10 NON-BLOCKING. Otherwise remarkably faithful: 11/11 spot-checks byte-verified, R-BCRYPT exactly encoded, 9/10 deferrals sound.

BLOCKING (rulings embedded):
B1. Realtime middleware: 4 route groups use realtimeAuthMiddleware (ek_ client-secret first, then standard) — not realtimeStandardAuthMiddleware. Missing 401 shape {"error":{"code":"invalid_realtime_client_secret","message":"Realtime client secret is invalid or expired","param":null,"type":"invalid_request_error"}}. RULING: fix section + add golden (ek_-prefixed garbage on GET /v1/realtime; recordable, zero issuance).
B2. oauth-callback ladder gaps: unparseable redirect_url 400 "invalid redirect_url" (RECORDABLE — golden required); 409 "oauth flow is not pending" (doc only); 500 "failed to persist oauth callback" via read-only auth dir (RECORDABLE — golden required); provider defaults to session provider before normalization (doc).
B3. Antigravity client_secret literal must be inlined (public installed-app constant, same caveat as other inlined constants): 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.
B4. Kimi expires_in: NO fallback branch — Kimi OMITS the field when device response omits it (xAI 1800 / Meta 900 keep their fallbacks). Fix exact field-set.
B5. device-auth-url egress-blocked 500s are deterministic and recordable (no credentials): RULING — record goldens for kimi/xai/meta; if recording proves nondeterministic, defer with the evidence.
B6. s3-mgmt-remote-disabled topology: RULING — option (a): pin gin trust-all-proxies XFF semantics as contract; re-record with an explicit X-Forwarded-For header (deterministic byte-replayable); ALSO state the replay topology requirement in the section + fixture notes. Client source IP enters the volatility/replay discussion.

NON-BLOCKING: N1 X-Cpa-Safe-Mode casing; N2 unconfigured-404 no X-Cpa-* cross-ref; N3 vertex auth-file row (or S6 delegation); N4 (folded B5) xai/meta 500 strings; N5 SetError TTL extension + bulk-complete one-liners; N6 HEAD warning pages; N7 case-insensitive gemini skip + camelCase refreshToken; N8 XFF pinning (ties B6); N9 ws-auth conditional bypass pointer to S5/S6; N10 unreachable 500 fine to omit.

COMPLIANCE: R-404 PASS; R-FIXTURE 38/38 goldens (gap=B5); R-SSE n/a; R-BCRYPT PASS. Deferred sanity 9/10 sound. Wire notes all incorporated + verified.
