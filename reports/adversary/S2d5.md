# Adversary review — S2d5 (oai2codex), gate round 1
Reviewer: adv-spec-5. VERDICT: FAIL — 2 BLOCKING (narrow, localized); all 24 fixtures verified PASS.

BLOCKING (with orchestrator rulings):
B1. Tool-schema enum normalization misclassified OPTIONAL: it is UNCONDITIONAL, default-ON (NormalizeCodexToolSchemas in BOTH Execute + ExecuteStream; pure-constant oneOf/anyOf unions with >=8 branches rewritten to enum lists; codexComplexUnionBranchThreshold=8; MCP-emitted schemas rationale). ORCHESTRATOR RULING: spec as MUST with exact trigger+effect from source; ADD one golden with an 8-branch union tool schema (recordable via mock; -> oracle-runner-3).
B2. §3.1 missing tool-call type selection for assistant history: client type:"function" whose function.name matches a DECLARED custom tool -> {"type":"custom_tool_call","input":<args>}; shared names removed from the custom set (function wins). Two sentences + (if cheap) fold into an existing golden or the new B1 golden.
NON-BLOCKING (apply): N1 E2 mechanism: modelCooldownError hardcodes 429 (not inherited) — fix parenthetical so S4 is not misled. N2 E6 empty-incomplete golden: AUTHORIZE recording (cheap, R-FIXTURE pressure). N3 cloaking runs LAST, overrides UA+Originator unconditionally — fix whitelist/ordering sentences (Originator-verbatim only with disable-codex-cloaking:true). N4 verbatim truncation bound: 256 runes (253+"..."). N5 §2.9 derivation: no assistant input; SHA-256(caller scope + format + system/developer<=50 runes + FIRST USER parts); assistant-only fallback api-key UUID. N6 response.done terminal alias note (websocket-only marker; out-of-scope rationale). N7 usage mapped from ANY event carrying it. N8 empty-args done edge emits NOTHING. N9 image chunk index: stream ALWAYS 0; non-stream sequential counter. N10 OPTIONAL-registry: add codex MultiAgentV2 (UA-gated); reasoning-replay cache is Claude-client-only (inert here); grok keepalive transform out-of-scope note. N11 tool_choice: empty type dropped; custom rebuilt to {type,name-shortened}; empty-name omits name. N12 E4 non-stream: terminal failure at ANY position => pre-commit error, nothing mid-body.
