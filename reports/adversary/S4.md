# Adversarial review — S4 (scheduling), gate round 1
Reviewer: adv-spec-1. VERDICT: FAIL — 4 BLOCKING (identity/classification contracts; numerically proven); strategy/ladder/retry/affinity/error-payload claims otherwise match source + goldens byte-for-byte. 20/20 fixtures verified.

BLOCKING (with orchestrator rulings):
B1. §2.2 credential-ID derivation WRONG for openai-compatibility + vertex families (contradicts own fixtures; proven: sha256("openai-compatibility:mock-openai" NUL key NUL base NUL "")[:12] = 2add2ed9fa51 matches all fixture auth_id_mappings). Actual per-family: openai-compatibility = idGen.Next("openai-compatibility:<lowercased-name>", key, base, proxyURL) — NO prefix/headers; empty name falls back to "openai-compatibility"; no api-key-entries => parts=(base); vertex:apikey = (key, base, proxyURL); gemini/claude/codex/xai/meta = (key, base, proxyURL, prefix, sorted-headers); interactions kind "gemini-interactions:apikey". RULING: fix to per-family part lists.
B2. auth_index seed: empty-api-key credentials seed = "id:<auth-ID>" fallback (proven: 2f8a144d9ae23286 = sha256("id:gemini:apikey:88c747d7b66d")[:16]); family prefixes are the literals gemini-api-key/claude-api-key/codex-api-key/xai-api-key/meta-api-key/interactions-api-key/openai-compatibility (proven vs S4-07). RULING: enumerate + id-fallback + path-attr nuance.
B3. WS-transport credential preference OMITTED: legacy preferCodexWebsocketAuths (codex-only) vs shard providerPrefersWebsocketTransport (codex+xai; WS view overrides priority tiers; pinnedAuthID="" exempt). Falsifies the "identical ordering semantics" note. RULING: MIRROR the preference including the legacy-vs-shard divergence (document both paths); the "observables must not differ" note is corrected to state the divergence precisely.
B4. Route-contextual overrides OMITTED: count_tokens endpoint-404 = availability-neutral (no 12h cooldown, SkipQuotaObservation); /responses/compact: request-fault stops at first credential + neutral set except credential-scoped/cloudflare/invalid_grant/401/402/403/429; store:false item-not-persisted 404 => request_scoped. RULING: add all three to §3.3/§5.
FOLLOW-UP RECORDING BATCH (authorized; -> oracle-runner-1): WS-preference case (codex websockets:true creds via codex-api-key mocks), count_tokens-404 neutrality case, /responses/compact fault case. All recordable.
NON-BLOCKING: N1 cloudflare ladder = quota ladder w/ 10s floor (state it); N2 legacy 4096-key cursor reset note; N3 stale cases.json S4-10 "4 hits" text; N4 reset-quota extra shapes optional; N5 cooldown_view verified positive.
