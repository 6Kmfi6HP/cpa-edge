# Adversary review — P0.2 (packages/core foundation slice)
Reviewer: adv-p0-core (fresh child, round 1). Probes under /tmp/cpa-probe/ (outside repo). No repo files modified.

VERDICT: FAIL — 1 blocking defect (B1); R1-R7 otherwise PASS; 8 non-blocking findings.

## BLOCKING
B1. update() resolves with the INTERNAL stored object — caller mutation silently corrupts stored state (detachment contract violated on the update-return path).
- Ref: R2 + store.ts:16-17 contract ("mutating a value after writing it ... never changes stored state").
- Code: store-memory.ts:119 returns writeDocument(...) which returns the stored reference (store-memory.ts:245-255); get/claim/ringRead clone on read; update is the only un-detached path (direct + retry).
- Probe probe6f.mjs: `c = await store.update('ns','k', () => ({a:1})); c.a = 99; get -> {a:99}` — same on contention/retry path. get() control clean.
- Existing detachment test only covers put/get.
- Fix shape: clone before returning from update(); note the path double-clones (accept() then writeDocument) so one clone can be dropped in the same fix.

## NON-BLOCKING (with orchestrator rulings, 2026-09-15)
N1. isJsonValue skips arrays' non-index own props: function-valued extra prop passes guard → put throws raw DataCloneError; object extra prop stored in memory but invisible on JSON wire. RULING: FIX NOW (validate arrays' non-index own enumerable props like objects).
N2. -0 accepted/preserved although not JSON-round-trip-stable. RULING: keep behavior, FIX DOC (guard doc states -0 is accepted, preserved in memory, normalizes to +0 on JSON-serializing backends; callers must not rely on -0).
N3. Guard-passing exotics surface raw non-Cpa errors: Proxy → DataCloneError; throwing getter → getter's error; ~50k nesting → RangeError. RULING: FIX NOW — wrap the put/update commit clone in try→CpaError('invalid-input') so all non-JSON writes fail as invalid-input; leave stack-depth RangeError documented rather than guarded.
N4. Envelope details freeze is shallow: nested objects alias caller originals; doc says "frozen envelope". RULING: keep shallow freeze (deep-freeze on hostile getters is unsafe), FIX DOC to say "shallow: top-level envelope+details frozen; nested values are the caller's responsibility".
N5. ack/release with null/undefined handle → raw TypeError instead of invalid-input (requireHandle dereferences before typeof check; requireName pattern is correct). RULING: FIX NOW (typeof-first check → CpaError invalid-input).
N6. Lease takeover boundary is <= (at deadline instant) while QueueClaim doc says "after which the lease may pass". RULING: keep <= (safe for at-least-once), FIX DOC to "at or after the deadline".
N7. isErrorEnvelope reads code/message through prototype chain (impossible from JSON.parse; soundness nit). RULING: doc note only.
N8. list() code-unit ordering vs UTF-8 byte order divergence for astral+private-use keys → remote stores must materialize+re-sort in-isolate. RULING: not a P0.2 change — carried into S6 spec + T2 review focus (portability note).

## Requirement audit (A1): R1 PASS / R2 FAIL-only-B1 / R3 PASS / R4 PASS / R5 PASS / R6 PASS / R7 PASS.
A5 runtime-agnostic: CLEAN — no node: imports/process/Buffer/fs in packages/core/src; only Web Standard APIs.
A6 commands: vitest packages/core 45/45; typecheck exit 0; lint clean; esbuild neutral bundle 10.6kb used for probes.

## Probes (A2) — highlights
P1: version-checked commit proven (ABA-by-value resisted, calls=2); tombstone race correct; 200 concurrent non-idempotent increments → exactly n(n+1)/2 callback invocations, final=200, no lost update; 30-round update-vs-delete torture: 0 anomalies.
P2: boundary takeover at ==leaseExpiresAt with fresh token; late-ack leniency without takeover honored; release restores FIFO position; hostile handles → invalid-input (except N5 null); frozen claims.
P3: concurrent appends with cycling capacities exact; shrink/grow windows exact; invalid capacities/limits/entries → invalid-input, state untouched.
P4: guard rejects bigint/symbol/function/Date/Map/Set/RegExp/cycles/holey arrays; accepts DAGs, null-prototype objects, symbol-KEYED props, 1000-deep.
P5: registry exactly 10 codes, frozen; full hostile-input constructor matrix → invalid-input; shallow freeze aliasing (N4); toJSON/envelope round-trip; no cause leak.
P6: falsy values (null/false/0/''/-0) fully distinguished from absent across all ops; queue payloads [null,false,0,''] delivered exactly; update cb returning undefined → invalid-input.
