# Adversary review — S1 (endpoint inventory), gate round 1
Reviewer: adv-spec-1. VERDICT: FAIL — 4 BLOCKING (all cheap), 8 NON-BLOCKING. Core inventory verified excellent: route registration matches upstream exactly; 24/24 fixtures byte-checked; all 5 recording corrections verified.

BLOCKING:
B1. §3.7 realtime capability routes claim 200s; upstream ALWAYS 501 via capability stubs (nested body: not_supported_error / realtime_capability_not_supported). Affected: GET+POST /v1/realtime/translations, translations/client_secrets, transcription_sessions, calls/:id/{accept,reject,refer}. Hangup is REAL (400/403/404) — spec wrongly lumps it with accept/reject/refer. [server_routes.go:87-100; capabilities.go:216-219]
B2. /v1/realtime* error envelope is the NESTED realtime envelope (writeRealtimeError: api_error>=500 / invalid_request_error 4xx / authentication_error 401; code realtime_request_failed), not the plain {"error":...} §6.3.6 assigns. 426 body code websocket_upgrade_required only WITHOUT ?call_id=; with call_id (incl. sideband) it is realtime_request_failed. client_secrets 400 nested invalid_request. [live.go writeLiveError; sideband.go; client_secret.go:225,412]
B3. §6.2 claude models id cloaking is CONFIG-CONDITIONAL: claude-code.disable-cloaking-model-list (default false) returns verbatim ids when true. Omitted in-scope variant. [code_handlers.go ClaudeModels; models.go BuildResponse]
B4. §3.2 image-route gate literal wrong: the value is bool true (DisableImageGenerationAll), not "all"; accepted values false/true/"chat"/"passthrough" (latter two keep /v1/images/* enabled). [disable_image_generation_mode.go; openai_images_handlers.go:606-609]

NON-BLOCKING:
N1. Pinning gaps (recordable behaviors claimed but no golden, no deferred marker): image-only-model 503 msg; /v1/images/* 400s; /v1beta/interactions 400s; POST /v1beta/models/ empty-action 400; mgmt 403 remote-disabled/key-not-set/IP-ban; oauth-callback 400 state-required; zstd decode-fail 400; open-when-empty api-keys; + post-fix realtime 501s / sideband 426 / client_secrets 400. RULING: ONE follow-up recording mission (S1-25 batch) authorized.
N2. S1-D3 safe-mode deferral weak: trigger is pure config (safemode example api-keys) — recordable via second config run. RULING: join S1-25 batch.
N3. pprof listener (pprof.enable, default 127.0.0.1:8316) — add one-liner. RULING: doc fix now.
N4. S1-21 HEAD fixture has duplicated header block artifact in "Body (stdout)"; contract layer treats HEAD bodies as empty. RULING: note in section + re-record S1-21 in S1-25 batch.
N5. oauth-callback 400 list omits "invalid body". RULING: doc fix.
N6. Add explicit R-SSE binding sentence (decoded event sequences, never chunk boundaries). RULING: doc fix.
N7. param not an ErrorDetail field (only hand-built model_not_found literal); plugin-present mgmt unknown-path runs auth before 404 (anchored no-plugin 404 holds); ?alt= (empty) => SSE framing; zstd multi-encoding decode order. RULING: doc fixes.
N8. Inventory-level looseness OK (S5 owns mgmt detail; S2 owns videos detail).

RULINGS COMPLIANCE: R-404 PASS (pinned S1-03/04/21); R-FIXTURE partial PASS (S1-D3 weak); R-SSE PASS w/ note N6; R-BCRYPT n/a. WIRE NOTES: all incorporated + fixture-pinned (verified). Fixture spot-check: 24/24 PASS (S1-21 HEAD artifact only).
