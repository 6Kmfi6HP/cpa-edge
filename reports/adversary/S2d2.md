# Adversary review — S2d2 (gem2oai), gate round 1
Reviewer: adv-spec-4. VERDICT: FAIL — 5 BLOCKING; fix quality otherwise high (sha256 tool-ids independently recomputed; all 20 goldens verified).

BLOCKING (with orchestrator rulings):
B1. "always SSE" is WRONG for :streamGenerateContent: exact "sse" (or no-alt) => SSE framing; ANY other alt value => RAW concatenated JSON chunks, no data: framing, no SSE headers, keep-alive force-disabled; `$alt` is an unmentioned fallback param. RULING: fix contract + record alt=json golden.
B2. Auth inventory: FIVE transports on /v1beta (Bearer incl. non-Bearer verbatim, X-Goog-Api-Key, X-Api-Key, ?key=, ?auth_token=); section covers 2. RULING: add all five + goldens for the 3 missing (401 shapes identical).
B3. Thinking pipeline runs AFTER translation on the ORIGINAL gemini body (ApplyRequestThinking): none->LOW, minimal->low, xhigh->high, max->high, unknown level -> 400 `level "X" not supported, valid levels: low, medium, high`; budget 0->low, 1-512->low, >=24577->high, < -1 -> 400 `budget N cannot be converted to a valid level`. RULING: add rows + >=2 goldens (one clamp, one 400).
B4. countTokens formula: segments = per-message role/name/content(text|image_url.url|audio id — audio data NOT counted)/tool_calls(id,type,function name+description+arguments+parameters)/tools/functions/tool_choice/response_format/input/prompt; TRIM each, join "\n"; tokenizer selection per model prefix (gpt-5*/gpt-4.1*/gpt-4o*/gpt-4*/gpt-3.5*/o1/o3/o4 -> per-model; empty -> cl100k_base; default o200k_base); thinking pipeline applies (bad thinking 400s countTokens). RULING: complete the formula + add tool-call countTokens golden.
B5. Case-1 request.http body is TRUNCATED invalid JSON (cases-file typo, faithfully recorded; upstream parsed it LENIENTLY via gjson best-effort). RULING: (a) re-record case 1 with a well-formed body; (b) register INTENTIONAL NON-EQUIVALENCE in SPEC §5: "malformed request bodies — upstream parses leniently (gjson best-effort, e.g. truncated JSON still translated); CPA-Edge rejects non-JSON bodies with 400 (strict boundary)"; contract tests replay well-formed bodies only.
NON-BLOCKING: N1 charset Content-Type precision (application/json; charset=utf-8 for c.JSON vs bare for WriteErrorResponse); N2 X-Cpa-Trace-Id only on executor-routed responses (gateway-local: none) — make §2.5 consistent; N3 GET /v1beta/models/{id} returns RAW 2-key registry map (no defaults applied — LIST-only asymmetry); N4 key-order blanket claim wrong for map-marshaled bodies; N5 multi-tool-call stream flush order NONDETERMINISTIC upstream (Go map iteration) — RULING: whitelist sorted-parts comparison for 2+ streamed tool calls (volatility entry, R-SSE-compatible); N6 Accept-Encoding gzip caveat + "exactly this set" => "when unconfigured"; N7 force-mapping per-model rewrites response model to alias (caveat the absolute); N8 429-without-Retry-After synthesizes 60s TPM window (client-visible via reset_seconds) — one line or S4 cross-ref; N9 non-stream-keep-alive-interval config-gated; N10 precision nits (2+ colons 404, thought-only turn msgIdx, FIFO explicit ids, reasoning object-without-text, empty-delta choice frame w/ usage).
New recordings -> oracle-runner-3 (original recorder).
