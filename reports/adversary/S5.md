# Adversarial review — S5 (management API), gate round 1
Reviewer: adv-spec-3. VERDICT: FAIL — 2 BLOCKING; otherwise strongest section yet: 143/143 route inventory exact, all deviations folded + fixture-backed, all 4 gate rulings honored, 22/22 fixtures verified.

BLOCKING (rulings embedded):
B1. §2.3 GET /model-definitions/:channel entry shape contradicts the recorded golden (struct-order full-field entries: id,object,created,owned_by,type,display_name,description,context_length,max_completion_tokens,supportedInputModalities,supportedOutputModalities,thinking?; e.g. {"id":"kimi-k2","object":"model","created":1752192000,...}). The 4-key alphabetical shape belongs to GET /auth-files/models ONLY (that one is correctly recorded). RULING: fix §2.3 to the recorded struct-order shape (or defer byte order to the golden); keep 4-key alphabetical only on /auth-files/models.
B2. vertex/import PEM-failure path labeled "(recorded)" with NO fixture step (the 8-step fixture uses a VALID synthetic PEM; validation order unpinned; side probe holds the exchange). RULING: RECORD the fake-PEM golden (400 {"error":"invalid service account","message":"private_key is not valid pem: missing pem markers"}) appended to tests/fixtures/S5/S5-vertex-import/ — oracle-runner-4 owns it (side probe at _cpa_edge_ref/run4/probes/S5/S5-vertex-import-drafted-content/). Keep the validation-order claim (source-verified: NormalizeServiceAccountMap before project_id check) and now pin it.
NON-BLOCKING (apply all): N1 step-count arithmetic = 167; N2 §8.3 mask-secrets: update to RESOLVED (mirror ruling, S7 round-2 amendment); N3 logs-enabled deferral: add cross-ref to tests/fixtures/S7/S7-10 (enabled-state logs goldens exist there); N4 auth-before-404 rule is method-agnostic (pluginManagementNoRoute); N5 vertex/import message family (missing private_key / pem decode failed / invalid rsa / invalid pkcs8 / not an RSA key / save_failed 500) + PKCS#8->PKCS#1 re-encoding on import (wire-visible on /auth-files/download; S6 note); N6 /v0/resource/plugins/** inventory line (noRoute-dispatched, no mgmt auth, unconditional 404-empty with plugins off); N7 header casing note (recorded Go-canonical X-Cpa-*, not X-CPA-*); N8 meta dynamic_fields duplicates.
