// @cpa-edge/executors — wire executors for the recorded request directions are
// embedded in their direction modules (SPEC.md R-EXECS); this package holds no
// implementations and contributes no runtime API yet.
export {}
